using UnityEngine;
using System.Collections;

public class initSetup : MonoBehaviour {

//	public string outSaveData = GameObject.Find ("button_done").GetComponent<pathwayAAScore>().outSave;

	// Use this for initialization
	void Start () {
//		if (outSaveData != "") {
//			// move objects to sockets
//		}	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
